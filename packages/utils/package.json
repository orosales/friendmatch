{
  "name": "@meetmates/utils",
  "version": "1.0.0",
  "description": "Shared utilities for MeetMates",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@meetmates/types": "file:../types"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
